name: Android CI (Full Build)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Extract the ZIP
      - name: Extract GitSafe project
        run: |
          echo "🔍 Checking for GitSafe.zip..."
          if [ -f "GitSafe.zip" ]; then
            mkdir extracted-project
            unzip -o GitSafe.zip -d extracted-project
            echo "✅ Unzipped GitSafe.zip into extracted-project/"
          else
            echo "❌ ERROR: GitSafe.zip not found!"
            exit 1
          fi

      # 3️⃣ Set up Java + Gradle
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v3

      # 4️⃣ Build APKs
      - name: Build Debug and Release APKs
        run: |
          cd extracted-project
          echo "🏗️ Running Gradle Wrapper build..."
          chmod +x gradlew || echo "No gradlew found — using system Gradle"
          ./gradlew clean assembleDebug assembleRelease --stacktrace || gradle clean assembleDebug assembleRelease --stacktrace

      # 5️⃣ Upload APKs
      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Built-APKs
          path: |
            extracted-project/app/build/outputs/**/*.apk