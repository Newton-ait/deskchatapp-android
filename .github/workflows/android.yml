name: Android CI (Full Build)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Extract your GitSafe.zip project
      - name: Extract project
        run: |
          echo "📦 Extracting GitSafe project..."
          mkdir -p extracted-project
          unzip -o GitSafe.zip -d extracted-project
          echo "✅ Extraction complete."

      # 3️⃣ Locate your project directory automatically
      - name: Locate project folder
        id: locate
        run: |
          echo "🔍 Searching for settings.gradle..."
          PROJECT_DIR=$(find extracted-project -type f -name "settings.gradle*" | head -n 1 | xargs dirname)
          if [ -z "$PROJECT_DIR" ]; then
            echo "❌ Could not locate a Gradle project."
            exit 1
          fi
          echo "✅ Found project: $PROJECT_DIR"
          echo "project_dir=$PROJECT_DIR" >> $GITHUB_OUTPUT

      # 🧠 NEW STEP: Static Gradle Pre-Check
      - name: Static Gradle Configuration Pre-Check
        run: |
          cd ${{ steps.locate.outputs.project_dir }}
          echo "🧠 Running Gradle configuration pre-check..."

          if [ ! -f "gradlew" ]; then
            echo "❌ Missing gradlew script!"
            exit 1
          fi
          if [ ! -f "build.gradle" ] && [ ! -f "build.gradle.kts" ]; then
            echo "❌ No root build.gradle file found!"
            exit 1
          fi
          if [ ! -f "settings.gradle" ] && [ ! -f "settings.gradle.kts" ]; then
            echo "❌ Missing settings.gradle file!"
            exit 1
          fi
          if [ ! -f "app/build.gradle" ] && [ ! -f "app/build.gradle.kts" ]; then
            echo "❌ No app module found!"
            exit 1
          fi
          echo "✅ Gradle structure looks good."

      # 4️⃣ Set up JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      # 5️⃣ Verify Gradle Wrapper
      - name: Verify Gradle Wrapper
        run: |
          cd ${{ steps.locate.outputs.project_dir }}
          echo "🧩 Verifying Gradle wrapper..."
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "⚠️ gradle-wrapper.jar missing — restoring official one..."
            mkdir -p gradle/wrapper
            curl -fsSL -o gradle/wrapper/gradle-wrapper.jar \
              https://raw.githubusercontent.com/gradle/gradle/master/gradle/wrapper/gradle-wrapper.jar
          fi
          chmod +x gradlew
          echo "✅ Gradle Wrapper verified:"
          ./gradlew --version

      # 6️⃣ Build Debug and Release APKs
      - name: Build Debug and Release APKs
        run: |
          cd ${{ steps.locate.outputs.project_dir }}
          echo "🚀 Building APKs..."
          chmod +x gradlew
          ./gradlew assembleDebug assembleRelease --stacktrace

      # 7️⃣ Upload generated APKs
      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: GitSafe-APKs
          path: |
            ${{ steps.locate.outputs.project_dir }}/app/build/outputs/apk/debug/*.apk
            ${{ steps.locate.outputs.project_dir }}/app/build/outputs/apk/release/*.apk