name: Android CI (Full Build)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3️⃣ Set up Android SDK
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # 4️⃣ Extract or prepare project
      - name: Extract or prepare project
        run: |
          echo "📦 Preparing project files..."
          if [ -f GitSafe.zip ]; then
            mkdir extracted-project
            unzip -q GitSafe.zip -d extracted-project
          else
            mkdir -p extracted-project
            rsync -av --exclude='extracted-project' --exclude='.git' ./ extracted-project/
          fi
          echo "✅ Extraction complete"
          ls -R extracted-project | head -n 20

      # 5️⃣ Check Gradle Wrapper
      - name: Check Gradle Wrapper
        run: |
          cd extracted-project/GitSafe
          echo "🧩 Checking Gradle wrapper JAR..."
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "❌ gradle-wrapper.jar missing — restoring official copy..."
            mkdir -p gradle/wrapper
            wget -q https://repo1.maven.org/maven2/org/gradle/gradle-wrapper/8.7/gradle-wrapper-8.7.jar -O gradle/wrapper/gradle-wrapper.jar
          fi
          ls -lh gradle/wrapper/gradle-wrapper.jar
          file gradle/wrapper/gradle-wrapper.jar
          echo "✅ Gradle wrapper confirmed"

      # 6️⃣ Make gradlew executable
      - name: Make gradlew executable
        run: |
          cd extracted-project/GitSafe
          chmod +x gradlew
          echo "✅ gradlew permissions fixed"

      # 7️⃣ Verify Gradle Wrapper
      - name: Verify Gradle Wrapper
        run: |
          cd extracted-project/GitSafe
          echo "🧩 Verifying Gradle wrapper..."
          ./gradlew --version

      # 8️⃣ Build Debug & Release APKs
      - name: Build Debug and Release APKs
        run: |
          cd extracted-project/GitSafe
          echo "🚀 Starting Gradle build..."
          ./gradlew clean
          ./gradlew assembleDebug
          ./gradlew assembleRelease
          echo "✅ Build completed"

      # 9️⃣ Upload built APKs
      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: all-apks
          path: |
            extracted-project/GitSafe/**/build/outputs/**/*.apk

      # 🔟 CI Summary
      - name: CI Summary
        run: |
          echo "--------------------------------------"
          echo "✅ Android CI Finished Successfully"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Branch: $GITHUB_REF_NAME"
          echo "--------------------------------------"